spring:
  application:
    name: eureka-server
server:
  port: 8761
# 由于该应用为注册中心，故该项设置为false，代表不向注册中心注册自己
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false